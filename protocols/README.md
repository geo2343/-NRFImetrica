# Protocol authority

Active sovereign protocol: `nrfimetrica_mother_v3_autonomous.json` / `NRFIMETRICA_MOTHER_V3_AUTONOMOUS`.

Active required process extensions:
- `sports_reasoning_packet_v2.json` / `NRFIMETRICA_SPORTS_REASONING_PACKET_V2`.
- `nrfimetrica_dual_status_v1.json` / `NRFIMETRICA_DUAL_STATUS_V1`.

Authority: `@NRFImetrica DOCUMENTO MADRE`, SHA-256 `d16896eba602af272117a5c83b56245aa201979d394301781d424e705b3642d3`.

Precedence: the latest sovereign reform inside the mother document governs earlier contradictory clauses. Current top reform: `A0-GOV.18 — REFORMA OPERATIVA SOBERANA V3`.

Kernel runtime: `NRFIM-KERNEL-0.8-DUAL-STATUS`.

The Sports Reasoning Packet extension does not replace A1–A8 and does not create a numerical score. It physically proves the separate causal sports judgment: tool calls, source families, evidence snapshots, claim→evidence lineage, bilateral first-inning reasoning, adversarial falsification, packet freeze/versioning, Drive persistence and process audit.

The Dual Status extension prevents a downstream technical block from being misreported as a sports conclusion. Sports judgment and execution authority are separate axes. `SPORTS_CANDIDATE` can coexist with `TECHNICAL_BLOCK`. `EXECUTABLE` requires the real A4→A8 authority chain. An A4/A6/A7 technical failure cannot rewrite a frozen NRFI sports candidate into `NO_PLAY`.

Every final report must state separately the sports-candidate count and the executable-candidate count, list the exact identities of all sports candidates, and include the dual status of every registered game. If sports candidates exist but none are executable, the report must identify `TECHNICAL_BLOCK_NOT_SPORTS_REJECTION` rather than presenting zero execution eligibility as a sports rejection.

A full-slate claim such as `15/15 analyzed` is generated from the latest packet state for every registered game. An incomplete game remains explicitly incomplete and does not stop the rest of the slate.

`nrfimetrica_v21_ai_analyst.json` is retained only as historical implementation evidence. It is **SUPERSEDED / NON-OPERATIVE** and must not be selected by runtime code.
